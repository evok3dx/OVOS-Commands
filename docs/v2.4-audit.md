# Living Jarvis audit · V3

**Source snapshot:** GitHub `main` at `3afc733` (2.3.1), compared on
24 September 2026 with the local tray/export source bundle, two machine
progress notes from 23–24 September, and the verified final Brain archive
`brain-v3-audit-20260924-205107.tar.gz` (17,298 source-file hashes checked).
The two attached Media and File Search packages were reviewed separately:
the final Brain archive contains the earlier file-search 0.1.9 and integrated
media implementation, not proof that either newer package was already installed.
The [final source reconciliation](v3-final-audit.md) lists each supplied item,
the installed-versus-source comparison, final corrections and remaining gates.
**VERIFIED** means code/test or a named
machine check; **PLANNED** means agreed work awaiting implementation;
**HISTORICAL** means the old finding was superseded. Source validation is not
live voice validation. Keep new findings here and durable choices in
[`12-decisions.md`](12-decisions.md).

| Finding | Status and evidence | Next check |
|---|---|---|
| Core Jarvis commands, Qwen fallback, app discovery, writing cue and Control Centre were missing from Git | **VERIFIED in candidate source:** `ovos_skill_jarvis_dispatcher/{routing_runtime,discovery,dictation}.py`, `scripts/{setup,control_center}.py`, `tray/ovos-tray.py`; local progress note reports user speech and GUI checks. | Run combined installer and real voice checks on Brain and laptop. |
| Dynamic Whisper app names and media cues | **VERIFIED on live Brain by user-run read-only check and current listener log (24 September 14:59:54):** `ovos-stt-plugin-fasterwhisper` selects `small.en`; the plugin compiles, its `execute()` forwards the dynamic prompt, and both installed patch files match the dynamic installer's recorded `after` SHA-256 values. A conservative standalone preview admitted 18 names, but the active listener reports **21 names, 61 tokenizer tokens, `limited=False`**. The bundled helper now matches the installed Brain helper, including the short media/writing cues; 17 offline patch tests pass. | Preserve the working plugin and helper. Test live recognition; do not assume the cues caused the earlier quiet-room false transcription. The owner handles and tests OVOS stack upgrades manually. |
| Historical two-icon finding | **VERIFIED in final Brain snapshot:** `source/user/.config/autostart/jarvis-mic-indicator.desktop` has both `Hidden=true` and `X-GNOME-Autostart-enabled=false`; `ovos-tray.desktop` is enabled, and `tray/ovos-tray.py` draws microphone status on its one icon. The old indicator file remains on disk for history only. V3 backs up and retires its legacy entry; rollback restores its disabled state without launching it. | Check the tray and rollback visually after an approved upgrade. Do not infer running icons from files existing on disk. |
| Brain/laptop OVOS versions differ | **VERIFIED in machine notes:** laptop last reported Workshop 7.0.6; Brain reports Workshop 9.8.7a1 and matching alpha components. Existing voice packages and configuration are preserved by `scripts/install.sh`. Fresh-install pins still describe 2.3.1's known baseline. | Run doctor and speech acceptance on both hosts. |
| V3 GUI update | **VERIFIED in source and isolated test:** Maintenance confirms once, then invokes `jarvis-update install --yes`; version comparison accepts `3.0.0rc1` followed by `3.0.0`. The previous GUI handoff launched an interactive prompt in a subprocess without a terminal. | Verify a published stable V3 update from the actual desktop before marking the release complete. |
| YouTube Music fully reliable | **HISTORICAL:** one end-to-end playback succeeded, but later provider search timed out and Google bot checks blocked extraction on a Proton exit. News provider was deliberately disabled. Media stays best effort. | Do not describe search success as audible playback. |
| Repeated unsolicited spoken `skill.error` | **VERIFIED in supplied raw log** `Pasted text(20260924-045106).txt`: at 01:47:14 and 01:49:53 `ovos_plugin_common_play.ocp.player.play()` rejects extracted NPR News Now media as an invalid playback request; OVOS then speaks `skill.error`. At 01:50:07 core reports a 300-second Common Play handler timeout. The log also shows a VLC callback on `NoneType.ocp_stop` at 01:46:44. This establishes the failure path, but not who requested the repeated NPR item or which provider/queue state triggered it. | Inspect the supplied OCP/VLC source and sanitised effective media config, identify the NPR request origin, and reproduce a fix on Brain. Do not misattribute this to Qwen. |
| Alpha-stack health check | **VERIFIED in Brain report:** user reported 42 PASS, one warning after local doctor edits; candidate `scripts/doctor.py` recognises exact documented alpha core/voice versions and retains the OpenWakeWord NumPy metadata warning. | Compare candidate doctor with Brain's installed doctor and rerun there. |
| Repeated app definitions and focused-window checks | **PLANNED:** previous 2.3.1 audit identified duplication. Local discovery and Qwen changes alter the baseline; do not refactor until current machine behaviour is captured. | Re-audit the combined candidate after host checks. |
| Settings export | **VERIFIED in local report:** user confirmed an archive and amber microphone dot; `scripts/settings_export.py` has a bounded allowlist. Restore from export has not been live-tested and remains manual. | Test a selected-file restore into an isolated home; design merge import separately. |
| Ultra 9 versus i7 Qwen | **VERIFIED only for Ultra 9 small sample:** correct-action median 3.25 s to 1.46 s after compact response. **PLANNED for i7:** same model trial and latency test. | Use `scripts/qwen-setup.py` read-only check, then run local voice sequence; change no GPU driver automatically. |
| General file search | **VERIFIED in older Brain snapshot:** separately installed `jarvis-file-search-skill 0.1.9`. **VERIFIED in 0.2.0 package and V3 tests:** 45 filename patterns, bounded results and guarded Qwen `files.search`; GUI reads installed skill phrases. Owner now reports the plugin works. | Repeat native and Qwen checks on the other computer; search never reads document contents. |
| Media plugin and title playback | **VERIFIED offline:** bounded `play {title}` parser, first YouTube result, Brave launch and five MPRIS controls. **VERIFIED in V3 source:** bundled plugin and tray phrase panel. Owner now reports the plugin works, after the older Brain snapshot. | Repeat spoken playback/stop on the other computer. Keep OCP playback issue separate. |

## Problems and resolutions to carry into V3

These are brief release-relevant records, not claims that the combined V3
candidate has passed on either host. The two 23–24 September machine progress
notes contain the chronology; `docs/06-ovos-voice.md` records voice/media state.

| Problem and evidence | Resolution or current state | V3 check |
|---|---|---|
| **Wake phrase failed after stack upgrade.** Removing OpenWakeWord based on older notes left the live `hey_jarvis` entry pointing to a missing plugin. Later Vosk detected audio but missed phrase windows. | **VERIFIED on Brain:** Vosk `time_between_checks` changed from `0.2` to `1.0` seconds and worked three times, then became intermittent. The working path uses an explicitly validated OpenWakeWord ONNX model with a `0.4` threshold. `1.0` was a timing trial, not a volume change or a permanent fix. | Preserve the Brain's installed wake model/configuration; verify a fresh install separately. `scripts/configure-wakeword.py` still has a `0.2` Vosk setting for custom phrases and does not recreate the Brain's explicit ONNX setup. |
| **Bella stopped speaking after the alpha upgrade.** Old PhōnNX lacked the current `opm.tts` integration and could not read newer voice metadata; the attempted wheel left inconsistent package metadata. An invented word produced `❓` despite a misleading nonempty-result test. | **VERIFIED on Brain:** repaired the PhōnNX source installation and applied the narrow ScriptConv/Misaki eSpeak fallback; `quorvex` now yields phonemes and was heard through live audio. The OpenWakeWord `numpy<2` metadata warning remains documented for the validated ONNX path. | Retain the functional unknown-word test from Brain's `scripts/doctor.py`; do not replace an existing voice stack or claim a clean `pip check`. |
| **Spoken Qwen app requests fell through.** DuckDuckGo medium fallback could claim them first; after pipeline ordering changed, the router still rejected the upgraded core's canonical `ovos.utterance.handle` topic. | **VERIFIED on Brain:** place the restricted router before medium fallback, keep native intents ahead, and accept both legacy and canonical utterance topics. Logs then matched three app commands to `jarvis.qwen.execute`; user confirmed apps opened. V3 now registers both local pipeline entry points and prompts to download its required model when missing. | Test command, question, command and disallowed actions on both hosts. Qwen actions remain limited to enabled, reviewed actions. |
| **Qwen was slower than necessary.** A paired Ultra 9 sample measured 3.25 s median before the shorter response format. | **VERIFIED on Brain only:** compact action response cut the small paired median to 1.46 s; model residency is requested. | Measure the five-year-old i7 before changing the eight-second timeout or acceleration; the Ultra 9 sample does not predict i7 latency. |
| **App names were misheard by `small.en`.** The enabled application set changes by machine. | **VERIFIED on Brain:** a bounded `initial_prompt` supplied enabled names and saved spoken names; seven reported voice transcriptions and 17 offline patch tests passed. It does not retrain Whisper. The first installer recorded the patched Faster-Whisper file's `after` SHA-256. Brain's later backup, taken after the alpha-stack upgrade and before the dynamic update, matches it byte for byte: `27aef90fb13d8749bce7d35b0464b6f9d39f5cb7c6330eaf9a026851f4e457b7`. Thus the stack upgrade left that original patch intact at the later checkpoint. The dynamic update then intentionally changed the plugin. Its installed helper, including the media cues, now matches the candidate. | Check spoken recognition after V3; do not treat the old checksum as the expected hash of the current dynamic file. OVOS stack upgrades remain manual and tested by the owner. |
| **“Start writing” was transcribed correctly but claimed by OCP.** The one-shot “Write this” flow also needed a short readiness cue. | **VERIFIED by user report:** place Adapt high before OCP high; “Write this” says Ready and types the next utterance after rechecking the focused window. “Read this” was added for selected text. | Preserve this pipeline order and focused-window guard; separately investigate the reported 3–4 second Start writing delay if it persists. |
| **Speech Note reading and media stop differed between Brain and candidate.** Earlier direct comparison exposed the missing serialised cancellation, spoken Stop and double-speed reading. A later package check found that the Brain's installed `jarvis-read-visible-text` accepted speed `2`, while its older source copy in the V3 candidate ignored that argument. The Brain's installed health-check also printed a summary absent from the repository copy. | **VERIFIED in candidate source and isolated test:** use the Brain's installed reading and health-check helpers; double-speed sets Speech Note's reviewed speed during reading and restores the machine's setting, refusing an absent or unsafe settings file. Preserve the Brain's cancellation, spoken Stop and vocabulary; the new Media skill receives explicit cancel and stop events. **Still pending live acceptance:** actual 2× playback, Speech Note interruption, OCP stop and Media plugin stop under a real listener. | Run spoken 2×, normal-speed restoration, barge-in and stop sequences on both machines before release. |
| **GUI update stalled after confirmation.** It launched a terminal-oriented prompt from a GUI subprocess with no terminal. | **VERIFIED in local isolated test:** Maintenance now calls `jarvis-update install --yes` after its own confirmation; release candidate version comparison was updated. | Test the real desktop update on an approved host before publication. |

## Release checks and outstanding work

1. Isolated source, fresh-install, upgrade-preservation, rollback and archive checks pass.
2. Reconcile the verified Brain snapshot's OCP/VLC, doctor and Whisper source and sanitised media settings with the candidate. The snapshot supplies installed OCP/VLC source but does **not** establish the origin or a proven fix for unsolicited NPR requests.
3. Check the bundled Media and File Search plugins, the GUI phrases, Whisper, spoken barge-in and stop, wake word, Bella, Qwen command/question/command, installer and GUI update on both machines. A passing offline test is not live voice evidence.
4. The owner has authorised V3 publication after confirming both plugins work.
   Items 2–3 are still open where no newer live result was supplied; publishing
   does not turn them into verified claims. Track any failures and ship a small
   corrective update rather than silently removing these checks.

## Documentation maintenance

Code and passing tests describe current behaviour. This audit and the decision
log describe intent and constraints. Never silently remove a security requirement
or unresolved item. Move material to `docs/history/` only with its historical
context; flag uncertainty here. Proposed deletions require a separate list and
evidence. Documentation changes accompanying this candidate are identified in
the commit; a broad historical cleanup remains separate.

**Proposed meaningful deletions from active README:** the 2.3.1 tray-menu
table and two-icon setup instructions, because the V3 tray and installer no
longer expose those controls in that place. The detailed one-machine diagnosis
and older install-version text also make the front page harder to use.
Their full wording is preserved in
[`history/readme-2.3.1.md`](history/readme-2.3.1.md), rather than silently
discarded. Current installer, preservation and rollback behaviour is in
[`07-installer-updates.md`](07-installer-updates.md),
[`09-backup-export.md`](09-backup-export.md), the security and maintenance
guides, and the source manifest. Retire an older guide only after separately
verifying its content against current code.
