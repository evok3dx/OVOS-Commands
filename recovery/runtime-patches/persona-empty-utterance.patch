--- a/ovos_persona/__init__.py
+++ b/ovos_persona/__init__.py
@@ -457,6 +457,14 @@
         Returns:
             IntentMatch if handled otherwise None.
         """
+        if (
+            not utterances
+            or not isinstance(utterances[0], str)
+            or not utterances[0].strip()
+        ):
+            LOG.debug("Ignoring empty persona query")
+            return None
+
         match = self.match_medium(utterances, lang, message)
         if match:
             return match
